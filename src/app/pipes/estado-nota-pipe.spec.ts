import { EstadoNotaPipe } from './estado-nota-pipe';

describe('EstadoNotaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoNotaPipe();
    expect(pipe).toBeTruthy();
  });
});
